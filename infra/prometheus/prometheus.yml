global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: ai-grader-api
    metrics_path: /api/metrics
    static_configs:
      - targets: ["api:8000"]
